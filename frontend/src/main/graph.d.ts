import React from "react";
import { GrapDataProps } from "./type";
type Props = {
    graph_data: GrapDataProps;
    children?: React.ReactNode;
};
export default function Graph({ graph_data }: Props): import("react/jsx-runtime").JSX.Element;
export {};
